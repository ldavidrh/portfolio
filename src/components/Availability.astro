<div class="availability"><div class="dot"></div>AVAILABLE FOR PROJECTS</div>

<style lang="scss">
  .availability {
    width: 100%;
    height: 120dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    font-size: clamp(2rem, 1.429rem + 2.857vw, 4rem);
    .dot {
      border-radius: 50%;
      background-color: greenyellow;
      width: clamp(2rem, 1.429rem + 2.857vw, 4rem);
      height: clamp(2rem, 1.429rem + 2.857vw, 4rem);
    }
  }
</style>
