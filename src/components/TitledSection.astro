---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<section class="xl:p-28">
  <h2 class="text-7xl mb-4">{title}</h2>
  {subtitle ? <p class="text-2xl text-gray-500 w-1/2"> {subtitle}</p> : null}
  <slot />
</section>

<script>
  import gsap from "gsap";
</script>
