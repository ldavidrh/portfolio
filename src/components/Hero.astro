<div class="hero">
  <div class="video-container">
    <video autoplay loop>
      <source src="video.mp4" type="video/mp4" />
    </video>
  </div>
  <h1>Colombia-based front-end developer & aspiring to be designer</h1>
</div>

<style lang="scss">
  .hero {
    position: relative;
    width: 100%;
    height: 100dvh;
    padding: 36px;

    .video-container {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;

      video {
        position: absolute;
        left: 50%;
        top: 50%;
        /* The following will size the video to fit the full container. Not necessary, just nice.*/
        min-width: 100%;
        min-height: 100%;
        -webkit-transform: translate(-50%, -50%);
        -moz-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);
        z-index: 0;
        object-fit: cover;
        filter: blur(10px);
      }
    }

    h1 {
      position: absolute;
      bottom: 64px;
      left: 36px;
      text-wrap: balance;
      font-size: 128px;
      max-width: 990px;
      font-weight: 500;
      mix-blend-mode: difference;
    }

    .small {
      font-size: 20px;
    }
  }
</style>
